# Giovanni's Dotfiles
I keep my configs here. Very basic for now, I expect it to grow over time. To install:

```
git clone git@github.com:gacrestani/dotfiles.git ~/.dotfiles
cd ~/.dotfiles
stow git
stow restic
```
